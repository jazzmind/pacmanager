# Living specifications

Core specs describe tested reference behavior only. Production targets live in docs/roadmap.md and docs/threat-model.md; they are not implemented requirements. Stable PAC IDs are referenced in independent tests. SpecGuard-compatible metadata/sections are used, but matching filenames is not proof of coverage.

